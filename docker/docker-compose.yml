services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      RELOAD: "false"
    volumes:
      - ./data:/app/data
      - ./models:/app/models